# Material-UI Setup - Инструкция

## ✅ Выполненные задачи

### 1. Установка и настройка Material-UI
- ✅ Установлены пакеты: `@mui/material`, `@emotion/react`, `@emotion/styled`, `@mui/icons-material`
- ✅ Создана кастомная тема с поддержкой светлой/тёмной темы

### 2. SimpleTechCard.jsx
- ✅ Компонент создан с использованием:
  - `Card`, `CardContent`, `CardActions`
  - `Typography` для текста
  - `Chip` для категории и статуса
  - `Button` для действий
- ✅ Реализованы функции:
  - `getStatusColor(status)` - возвращает цвет чипа
  - `getStatusText(status)` - возвращает русский текст статуса
- ✅ Используется `sx` prop для стилизации
- ✅ Условный рендеринг кнопок в зависимости от статуса

### 3. Dashboard.jsx
- ✅ Компонент панели управления создан с:
  - `AppBar` и `Toolbar` для шапки
  - `Tabs` и `Tab` для навигации
  - `Grid` для адаптивной раскладки
  - Статистическими карточками с иконками
  - `LinearProgress` для визуализации прогресса
- ✅ Компонент `TabPanel` для содержимого вкладок
- ✅ Расчет статистики из массива технологий
- ✅ `Badge` для уведомлений на вкладках

### 4. Настройка темы Material-UI
- ✅ Кастомная тема создана с `createTheme`
- ✅ Настроены цвета `primary` и `secondary`
- ✅ Приложение обернуто в `ThemeProvider`
- ✅ Добавлен `CssBaseline` для нормализации стилей

### 5. Самостоятельная работа

#### Задание 1: Компонент уведомлений Snackbar
- ✅ Создан `NotificationSnackbar.jsx`
- ✅ Поддержка типов: success, error, warning, info
- ✅ Автоматическое закрытие
- ✅ Адаптивность для всех размеров экранов
- ✅ Интерактивные элементы (кнопка закрытия)

#### Задание 2: Переключение темы (светлая/тёмная)
- ✅ Использован `ThemeProvider` и `createTheme`
- ✅ Выбор темы сохраняется в `localStorage`
- ✅ Тема применяется ко всем компонентам
- ✅ Сохранение темы между перезагрузками

#### Задание 3: Адаптивность и корректность
- ✅ Адаптивный дизайн с `Grid` и `Box`
- ✅ Корректная работа на всех размерах экранов
- ✅ Доступность (a11y) через Material-UI компоненты

## 📁 Структура файлов

```
src/
├── theme/
│   └── theme.js                    # Кастомная тема Material-UI
├── contexts/
│   ├── ThemeContext.jsx            # Контекст для управления темой
│   └── NotificationContext.jsx     # Контекст для уведомлений
├── components/
│   ├── SimpleTechCard.jsx          # Карточка технологии (Material-UI)
│   ├── Dashboard.jsx               # Dashboard с вкладками
│   └── NotificationSnackbar.jsx    # Компонент уведомлений
└── pages/
    └── Dashboard.jsx               # Страница Dashboard
```

## 🚀 Использование

### Переключение темы
Тема переключается через кнопку в навигации. Предпочтение сохраняется в localStorage.

### Использование уведомлений
```javascript
import { useNotification } from '../contexts/NotificationContext';

const MyComponent = () => {
  const { showSuccess, showError, showWarning, showInfo } = useNotification();
  
  const handleAction = () => {
    showSuccess('Операция выполнена успешно!', 'Успех');
  };
};
```

### Использование SimpleTechCard
```javascript
import SimpleTechCard from '../components/SimpleTechCard';

<SimpleTechCard
  technology={tech}
  onStatusChange={(id) => handleStatusChange(id)}
  onDetailsClick={(id) => navigate(`/technology/${id}`)}
/>
```

### Доступ к теме
```javascript
import { useTheme } from '../contexts/ThemeContext';

const MyComponent = () => {
  const { mode, toggleTheme, theme } = useTheme();
  // mode: 'light' | 'dark'
  // toggleTheme: функция для переключения
  // theme: объект темы Material-UI
};
```

## 📝 Важные заметки

1. Все компоненты Material-UI используют кастомную тему
2. Тема автоматически применяется через `ThemeProviderWrapper`
3. `CssBaseline` нормализует стили браузера
4. Все цвета адаптированы для светлой и тёмной темы

## 🎨 Цветовая схема

### Тёмная тема (по умолчанию)
- Primary: `#4ecdc4` (бирюзовый)
- Secondary: `#45b7d1` (голубой)
- Background: `#0a0a0a` (очень тёмный)

### Светлая тема
- Primary: `#1976d2` (синий)
- Secondary: `#dc004e` (розовый)
- Background: `#f5f5f5` (светло-серый)

